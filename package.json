{
  "name": "lingo-pr-action",
  "version": "1.0.0",
  "description": "GitHub Action that summarizes diff and translates to Arabic & Hebrew",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx esbuild src/index.js --bundle --platform=node --outfile=dist/index.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.0",
    "openai": "^4.50.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}